### Operating systems ###
# macOS
.DS_Store
._*

# Windows
Thumbs.db
[Dd]esktop.ini

# Linux
*~

### Node ###
node_modules/

# logs, including the ones npm and yarn write when an install fails
*.log

# test coverage, if you add a test runner that reports it
coverage
*.lcov

# standard lints with eslint under the hood, and eslint caches what it has already checked
.eslintcache

# environment variables, which is where secrets belong rather than in your code
.env
.env.test

### Roosevelt ###
# everything roosevelt builds, all of which is generated from your statics and can be rebuilt at any time
.build
public

# your certificates, session secret, and anything else you would not want in a repo
<%= secretsPath %>

# the session store, which is written as your app runs
sessions.sqlite*
